I added a folder called `realism_engine/` to this Sales Pro repo. Please read `realism_engine/claude/CLAUDE_IMPLEMENTATION_PROMPT.md` and implement it carefully.

Do not rewrite the whole app. Add realistic script generation as a new mode or new command first. Keep the current video ingestion working.

Main goal: make the script generator evidence-aware, realistic, and safe from fake SEO claims. It must use the seven prompt files in `realism_engine/prompts/`, the proof inventory, approved claim bank, and banned claim bank.

After implementation, run a syntax check and show me the exact command to test the realistic script generator.
